{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main window (core:default only — deny-by-default, no broad filesystem/shell/process/http grants).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
