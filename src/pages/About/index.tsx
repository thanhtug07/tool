export default function AboutPage() {
  return (
    <section aria-labelledby="about-heading">
      <h1 id="about-heading" className="text-lg font-semibold">
        About
      </h1>
      <p className="mt-1 text-sm text-muted-foreground">
        AI Video Localization Studio — desktop app for video localization (STT, translation,
        subtitles).
      </p>
    </section>
  );
}
